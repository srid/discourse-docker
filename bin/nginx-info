#!/bin/bash

set -e

ID=$(sudo docker ps | grep srid/nginx:1.3 | awk '{print $1}')

HOSTIP=$(/sbin/ifconfig eth0 | grep "inet addr" | awk -F: '{print $2}' | awk '{print $1}')
PORT=$(sudo docker port $ID 80)

# echo "# password: ..."
echo "Discourse is running at http://$HOSTIP:$PORT/"
